import { CountingDirective } from './counting.directive';

describe('CountingDirective', () => {
  it('should create an instance', () => {
    const directive = new CountingDirective();
    expect(directive).toBeTruthy();
  });
});
